# .NET build artifacts
bin/
obj/
*.user
*.suo
.vs/

# Rust legacy build artifacts (local only)
target/

# ONNX models (large; not for git).
# NEVER commit yolo26DLA.onnx — Ultralytics asserts AGPL-3.0 on YOLO-trained
# weights, which is incompatible with this project's MIT licence. The wildcard
# below covers it, plus tiny_layout.onnx and any future model artefacts.
models/*.onnx

# Project-local
config.json
TODO.md

# Installer build artifacts
installer/output/
installer/icon.ico

# IDE
.idea/
*.swp
*~

# PDFs for experiment
experiments/PDFs/
